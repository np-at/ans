version: 0.1
cli:
  version: 0.16.1-beta
lint:
  enabled:
    - git-diff-check@SYSTEM
    - gitleaks@8.11.2
    - prettier@2.7.1
    - ansible-lint@6.5.1
  triggers:
    - linters:
        - ansible-lint
      paths:
        - playbooks
      targets:
        - playbooks
